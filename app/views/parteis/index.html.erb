<h1>Parteien anzeigen</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Beschreibung</th>
    <th>Homepage</th>
  </tr>

<% for partei in @parteien %>
  <tr>
    <td><%=h partei.name %></td>
    <td><%=h partei.decription %></td>
    <td><%= link_to partei.homepage, partei.homepage %></td>
    <td><%= link_to 'Anzeigen', partei %></td>
    <td><%= link_to 'Bearbeiten', edit_partei_path(partei) %></td>
    <td><%= link_to 'Löschen', partei, :confirm => 'Bist du sicher?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Neue Partei anlegen', new_partei_path %>
