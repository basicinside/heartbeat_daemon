<h3>Land bearbeiten</h3>

<% form_for(@land) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.label 'Beschreibung' %><br />
    <%= f.text_field :description %>
  </p>
  <p>
    <%= f.submit "Speichern" %>
  </p>
<% end %>

<%= link_to 'Anzeigen', @land %> |
<%= link_to 'Zur Übersicht', lands_path %>
